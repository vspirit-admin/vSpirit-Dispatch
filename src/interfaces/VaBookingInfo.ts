export default interface VaBookingInfo {
  callsign: string
  flightNumber: string
  altitude: number,
  costIndex: string,
  passengers: number,
  cargo: number,
  network: string,
  type: string,
}
